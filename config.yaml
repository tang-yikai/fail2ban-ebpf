ssh:
  port: 22 # SSH 监听端口

xdp:
  iface: ens33 

ban:
  threshold: 3 # 在统计窗口内达到该失败次数则封禁
  window_minutes: 10 # 失败统计窗口，单位分钟
  duration_minutes: 10 # 封禁时长，单位分钟；0 表示不自动解封
  max_blocked_ips: 65536 # XDP 黑名单 map 最大容量

log:
  file: ./logs/fail2ban-ebpf.log # 成功/失败/封禁/解封/启动/退出日志文件
